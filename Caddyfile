# Reverse proxy with automatic Let's Encrypt for the optional `tls` compose profile.
# Set CADDY_DOMAIN in .env and PUBLIC_BASE_URL=https://<that domain>.
{$CADDY_DOMAIN} {
	encode zstd gzip
	reverse_proxy bot:8080
}
